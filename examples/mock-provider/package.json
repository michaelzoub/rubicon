{
  "name": "@rubicon/mock-provider",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@rubicon/core": "workspace:*",
    "@rubicon/provider-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
